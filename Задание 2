Нет, послать запрос GET с Body нельзя, поскольку по стандартам GET не имеет тела, 
Нужно воспользоваться PUT или POST, а через GET можно передать данные либо в заголовке, либо в пути
Сами стандарты не запрещают использовать GET, но ни один браузер по-просту этого не умеет
